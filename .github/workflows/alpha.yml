- name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install numpy<2.0.0  # Force a compatible math engine
          pip install pandas<3.0.0 # Force a compatible data engine
          pip install pandas-ta --no-dependencies # Install the logic without the headache
          pip install -r requirements.txt
